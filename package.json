{
  "name": "localparse",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first browser app for inspecting JSON and JSONL files.",
  "type": "module",
  "license": "PolyForm-Noncommercial-1.0.0",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
